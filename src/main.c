#include "util.c"

void render(void) {
  int size = 10 * sizeof(u8);
  // printf("yo\n");
  // void *buffer = malloc(size);
}

int main(void) {
  int size = 10 * sizeof(u8);
  // printf("yo\n");
  // void *buffer = malloc(size);
  return 0;
}
